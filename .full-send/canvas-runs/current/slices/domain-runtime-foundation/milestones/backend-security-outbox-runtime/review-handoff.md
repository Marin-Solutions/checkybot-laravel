Review completed for backend-security-outbox-runtime. Verification commands reproduced the ledger, but AC-domain-runtime-foundation-9 fails: duplicate queued jobs can bypass retry backoff because worker-side processing ignores available_at, and the existing retry/backoff evidence directly invokes FoundationEventProcessor instead of proving the behavior through the real relay plus queue:work runtime. Changes requested; see review.md.