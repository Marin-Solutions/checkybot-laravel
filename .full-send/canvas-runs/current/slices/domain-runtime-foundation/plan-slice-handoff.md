Proposed the domain-runtime-foundation slice specification.

Artifacts:
- `.full-send/canvas-runs/current/slices/domain-runtime-foundation/slice-spec.json` — machine contract
- `.full-send/canvas-runs/current/slices/domain-runtime-foundation/slice-spec.md` — human specification

The plan preserves inherited ownership, defines three backend milestones with 14 stable binary criteria, declares the authenticated nine-number status-summary API and test-only seam APIs, covers all eight run-plan-owned seams through real API/relay/queue/API acceptance paths, and assigns unique slice-local milestone artifact paths. No product frontend milestone is emitted because this foundation slice owns shared contracts but no product UI files or screens.
